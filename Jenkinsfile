pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself, keep on moving!'
                sh 'python --version'
                sh 'cat /etc/os-release'
            }
        }
    }
}