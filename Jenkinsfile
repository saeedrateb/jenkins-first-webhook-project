pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself, go get it, today!'
                sh 'python --version'
                sh 'cat /etc/os-release'
            }
        }
    }
}